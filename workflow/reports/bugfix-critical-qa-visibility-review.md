Фикс в `qaTasksAtom` корректный: фильтрация по `completed_at === null` оставляет задачи в QA до фактического завершения и устраняет их преждевременное исчезновение после взятия в работу.
Конфликтов с другими табами не выявлено: `Current` по-прежнему определяется через `isTaskActive`, а `Completed` — через `isTaskCompleted`, поэтому завершённые задачи не возвращаются в QA.
Локальная проверка `pnpm lint` и `pnpm build` проходит успешно; исправление одобрено.
